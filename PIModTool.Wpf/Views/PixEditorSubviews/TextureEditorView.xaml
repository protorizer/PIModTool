<views:MvxWpfView
    xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
    xmlns:mvx="clr-namespace:MvvmCross.Platforms.Wpf.Binding;assembly=MvvmCross.Platforms.Wpf"
    x:Class="PIModTool.Wpf.Views.PixEditorSubviews.TextureEditorView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:helix="http://helix-toolkit.org/wpf"
      xmlns:local="clr-namespace:PIModTool.Wpf.Views.PixEditorSubviews"
      xmlns:components="clr-namespace:PIModTool.Wpf.Views.Components"
      xmlns:utilities="clr-namespace:PIModTool.Wpf.Utilities"
      mc:Ignorable="d" 
      d:DesignHeight="720" d:DesignWidth="1280">

    <views:MvxWpfView.Resources>
        <utilities:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" True="Visible" False="Collapsed"/>
        <utilities:BooleanToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" True="Collapsed" False="Visible"/>
    </views:MvxWpfView.Resources>

    <TabControl Background="Transparent" SelectedIndex="{Binding SelectedTab}">
        <TabItem>
            <TabItem.Header>
                <TextBlock Text="Standard Texture" Foreground="LightGray"/>
            </TabItem.Header>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="1"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Vertical" Grid.Column="0">
                    <TextBlock Text="Options" Foreground="LightGray" HorizontalAlignment="Center" FontFamily="pack://application:,,,/Resources/Fonts/#Euromode" FontSize="14" Padding="10"/>
                    <Button Height="25" Width="150" Style="{StaticResource CustomButtonStyle}" Margin="0,0,0,5" Command="{Binding ReplaceTextureCommand}">Replace Texture</Button>
                </StackPanel>
                <Rectangle Grid.Column="1" Fill="DarkGray"/>
                <components:TextureView Grid.Column="2" Source="{Binding ActiveTexture, Converter={StaticResource BinaryToBitmapConverter}}"/>
            </Grid>
        </TabItem>
        <TabItem>
            <TabItem.Header>
                <TextBlock Text="HD Texture" Foreground="LightGray"/>
            </TabItem.Header>
            <Grid>
                <Button Width="200" Height="75" Command="{Binding OpenPitCommand}" Visibility="{Binding LoadedPit, Converter={StaticResource InverseBoolToVisibilityConverter}}" Style="{StaticResource CustomButtonStyle}">Open textures.pit</Button>
                <Grid Visibility="{Binding LoadedPit, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="1"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Vertical" Grid.Column="0">
                        <TextBlock Text="Options" Foreground="LightGray" HorizontalAlignment="Center" FontFamily="pack://application:,,,/Resources/Fonts/#Euromode" FontSize="14" Padding="10"/>
                        <Button Height="25" Width="150" Style="{StaticResource CustomButtonStyle}" Margin="0,0,0,10" Command="{Binding ReplaceHDTextureCommand}">Replace Texture</Button>
                        <Button Height="25" Width="150" Style="{StaticResource CustomButtonStyle}" Margin="0,0,0,10" Command="{Binding SavePitCommand}">Save textures.pit</Button>
                    </StackPanel>
                    <Rectangle Grid.Column="1" Fill="DarkGray"/>
                    <components:TextureView Grid.Column="2" Source="{Binding ActiveTextureHD, Converter={StaticResource BinaryToBitmapConverter}}"/>
                </Grid>
            </Grid>
        </TabItem>
    </TabControl>

</views:MvxWpfView>
