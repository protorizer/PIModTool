<views:MvxWpfView
    xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
    xmlns:mvx="clr-namespace:MvvmCross.Platforms.Wpf.Binding;assembly=MvvmCross.Platforms.Wpf"
    x:Class="PIModTool.Wpf.Views.Components.AudioStemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PIModTool.Wpf.Views.Components"
             xmlns:utilities="clr-namespace:PIModTool.Wpf.Utilities"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="275">
    <views:MvxWpfView.Resources>
        <utilities:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" True="Visible" False="Collapsed"/>
        <utilities:BooleanToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" True="Collapsed" False="Visible"/>
    </views:MvxWpfView.Resources>
    <Grid>
        <Button Style="{StaticResource CustomButtonStyle}" Command="{Binding PickFileCommand}" Visibility="{Binding IsFilePicked, Converter={StaticResource InverseBoolToVisibilityConverter}}">Click to open .wav</Button>
       
        <local:AudioWaveformView Width="275" Height="100" Peaks="{Binding AudioPeaks}" IsPlaying="{Binding IsPlaying}" PlayheadPosition="0" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsFilePicked, Converter={StaticResource BoolToVisibilityConverter}}"/>
        <Button Opacity ="0" Command="{Binding PickFileCommand}" Visibility="{Binding IsFilePicked, Converter={StaticResource BoolToVisibilityConverter}}"/>
    </Grid>
</views:MvxWpfView>
