<UserControl x:Class="PIModTool.Wpf.Views.Components.FileTreeNodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PIModTool.Wpf.Views.Components"
             xmlns:utilities="clr-namespace:PIModTool.Wpf.Utilities"
             xmlns:dd="urn:gong-wpf-dragdrop"
             mc:Ignorable="d" 
             x:Name="RootControl" d:Width="100" d:Height="500">
    <UserControl.Resources>
        <utilities:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" True="Visible" False="Collapsed"/>
        <utilities:BooleanToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" True="Collapsed" False="Visible"/>
        <utilities:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>

        <!-- Menu for right clicking on a node -->
        <ContextMenu x:Key="NodeContextMenu" Visibility="{Binding EnableRename, Source={x:Reference RootControl}, Converter={StaticResource BoolToVisibilityConverter}}">
            <MenuItem Header="New File…"   Click="NewFileMenuItem_Click" Visibility="{Binding NewFileCommand, Source={x:Reference RootControl}, Converter={StaticResource NullToVisibilityConverter}}" />
            <MenuItem Header="Rename"      Click="RenameMenuItem_Click" Visibility="{Binding EnableRename, Source={x:Reference RootControl}, Converter={StaticResource BoolToVisibilityConverter}}"/>
            <MenuItem Header="Export File" Click="ExportMenuItem_Click" Visibility="{Binding ExportFileCommand, Source={x:Reference RootControl}, Converter={StaticResource NullToVisibilityConverter}}"/>
        </ContextMenu>
        
        <!-- Menu for right clicking on an empty space -->
        <ContextMenu x:Key="TreeContextMenu" Visibility="{Binding EnableContextMenu, Source={x:Reference RootControl}, Converter={StaticResource BoolToVisibilityConverter}}">
            <MenuItem Header="New File…" Click="NewFileMenuItem_Click"  Visibility="{Binding NewFileCommand, Source={x:Reference RootControl}, Converter={StaticResource NullToVisibilityConverter}}"/>
        </ContextMenu>

        <!-- Node data template -->
        <HierarchicalDataTemplate x:Key="TreeNodeTemplate"
                                  ItemsSource="{Binding Children}">
            <StackPanel Orientation="Horizontal" Margin="0,1">
                <TextBlock Text="📁" Margin="0,0,4,0" VerticalAlignment="Center"
                           Visibility="{Binding IsFolder, Converter={StaticResource BoolToVisibilityConverter}}" />
                <TextBlock Text="📄" Margin="0,0,4,0" VerticalAlignment="Center"
                           Visibility="{Binding IsFolder, Converter={StaticResource InverseBoolToVisibilityConverter}}" />

                <!-- Static display -->
                <TextBlock Text="{Binding Name}"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"
                           ToolTip="{Binding Name}"
                           Visibility="{Binding IsRenaming, Converter={StaticResource InverseBoolToVisibilityConverter}}" />

                <!-- Inline rename editor -->
                <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         MinWidth="80" Padding="1"
                         Visibility="{Binding IsRenaming, Converter={StaticResource BoolToVisibilityConverter}}"
                         KeyDown="RenameBox_KeyDown"
                         LostFocus="RenameBox_LostFocus"
                         Loaded="RenameBox_Loaded" />
            </StackPanel>
        </HierarchicalDataTemplate>

    </UserControl.Resources>

    <TreeView x:Name="FileTree"
              ItemsSource="{Binding RootNodes}"
              ItemTemplate="{StaticResource TreeNodeTemplate}"
              ContextMenu="{StaticResource TreeContextMenu}"
              SelectedItemChanged="FileTree_SelectedItemChanged"
              dd:DragDrop.UseDefaultDragAdorner="True"
              Background="Transparent"
              BorderThickness="0"
              Foreground="LightGray">
        <TreeView.ItemContainerStyle>
            <Style TargetType="TreeViewItem">
                <Setter Property="IsExpanded"  Value="{Binding IsExpanded,  Mode=TwoWay}" />
                <Setter Property="IsSelected"  Value="{Binding IsSelected,  Mode=TwoWay}" />
                <Setter Property="Foreground"  Value="LightGray" />
                <Setter Property="ContextMenu" Value="{StaticResource NodeContextMenu}" />
            </Style>
        </TreeView.ItemContainerStyle>
    </TreeView>
</UserControl>
