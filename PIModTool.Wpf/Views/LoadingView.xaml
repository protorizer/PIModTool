<views:MvxWpfView
    xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
    xmlns:mvx="clr-namespace:MvvmCross.Platforms.Wpf.Binding;assembly=MvvmCross.Platforms.Wpf"
    x:Class="PIModTool.Wpf.Views.LoadingView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PIModTool.Wpf.Views"
      mc:Ignorable="d" 
      d:DesignHeight="720" d:DesignWidth="1280">

    <Grid>
        <Image Source="pack://application:,,,/Resources/Images/LoadingBase.png" Width="1280"/>

        <Rectangle HorizontalAlignment="Center" VerticalAlignment="Center" Width="300" Height="250" IsHitTestVisible="False">
            <Rectangle.Fill>
                <LinearGradientBrush>
                    <GradientStop x:Name="LeftStop" Color="#FF000000" Offset="1" />
                    <GradientStop x:Name="MiddleStop" Color="#FF0000FF" Offset="1.5" />
                    <GradientStop x:Name="RightStop" Color="#FF000000" Offset="2" />
                </LinearGradientBrush>
            </Rectangle.Fill>
            <!-- Opacity mask uses both logo and text -->
            <Rectangle.OpacityMask>
                <VisualBrush Stretch="Uniform">
                    <VisualBrush.Visual>
                        <StackPanel x:Name = "LoadingLogo" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Image Source="pack://application:,,,/Resources/Images/PIIconSolid.png" Width="250"/>
                            <TextBlock Text="{Binding Message, RelativeSource={RelativeSource AncestorType=UserControl}}" Foreground="Black" HorizontalAlignment="Center" FontFamily="pack://application:,,,/Resources/Fonts/#Euromode" FontSize="65"/>
                        </StackPanel>
                    </VisualBrush.Visual>
                </VisualBrush>
            </Rectangle.OpacityMask>

            <Rectangle.Resources>
                <Storyboard x:Key="ShimmerStoryboard" RepeatBehavior="Forever">
                    <DoubleAnimation Storyboard.TargetName="LeftStop"
                             Storyboard.TargetProperty="Offset"
                             From="-1.0" To="1"
                             Duration="0:0:2" />
                    <DoubleAnimation Storyboard.TargetName="MiddleStop"
                             Storyboard.TargetProperty="Offset"
                             From="-0.5" To="1.5"
                             Duration="0:0:2" />
                    <DoubleAnimation Storyboard.TargetName="RightStop"
                             Storyboard.TargetProperty="Offset"
                             From="0.0" To="2"
                             Duration="0:0:2" />
                </Storyboard>
            </Rectangle.Resources>

            <!-- Automatically start shimmer when loaded -->
            <Rectangle.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard Storyboard="{StaticResource ShimmerStoryboard}" />
                </EventTrigger>
            </Rectangle.Triggers>
            
        </Rectangle>
    </Grid>
</views:MvxWpfView>
